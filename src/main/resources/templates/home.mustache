{{> header}}
{{> navbar}}

<div class="container">
    <h1>{{title}}</h1>
    <form action="/" method="get">
        <div class="form-group">
            <label for="url"></label> <input type="text"
                                             class="form-control" id="url" name="url" placeholder="Search.."/>
        </div>
        <button type="submit" class="btn btn-primary">Search</button>
    </form>
</div>
{{> footer}}